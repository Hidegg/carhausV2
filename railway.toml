[build]
builder = "nixpacks"

[deploy]
startCommand = "python3 -m flask db upgrade && python3 -m gunicorn run:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
